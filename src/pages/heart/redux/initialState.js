const initialState = {
  heart: {
    needReloadList: true,
    listData: [],

    addPending: false,
    addError: null,
    listPending: false,
    listError: null,
  }
};

export default initialState;
