const initialState = {
  water: {
    needReloadList: true,
    listData: [],

    addPending: false,
    addError: null,

    listPending: false,
    listError: null,

    delPending: false,
    delError: null,
  }
};

export default initialState;
